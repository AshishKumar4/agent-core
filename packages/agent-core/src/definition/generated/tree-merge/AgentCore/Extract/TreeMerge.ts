/*
 * Generated by TSLean from kernel-checked Lean declarations.
 * Fragment: tslean-semantic-typed-v5
 * Lean module: AgentCore.Extract.TreeMerge
 * Generated module: AgentCore/Extract/TreeMerge.ts
 * Package entry module: AgentCore.Extract.TreeMerge
 * Semantic identity: sha256:46e0f25c2cc82cf0a570871ddb0f3c6bce91991de4be8439bc8efc55030dfcf8
 * Input closure: sha256:d33451612dc64efbd70349eb720b7773f8d6f6be8b9e726dda17fc0cf90fe8de
 * Semantic IR: sha256:c144364a5b2ef370a43296dda5fa50f017a11610f13896c65e1e3a1142641d89
 * Generated module body: sha256:bb5b6f6bf4b7f4a97e4cad64ff1767a43c7ffc22d69caf54128cdeb8747dae3f
 * Generated package body: sha256:9f76c340f5352785e1f609f4b77463948808c22f9ba1ffd99262cb411c678a21
 * Lean toolchain: leanprover/lean4:v4.33.1
 * Lean: Lean (version 4.33.1, x86_64-unknown-linux-gnu, commit 819816b2e0a3bf405af45ae5c7af2491d8f5bee6, Release)
 * Lake: Lake version 5.0.0-src+819816b (Lean version 4.33.1)
 * Runtime certificates: sha256:4f53cda18c2baa0c0354bb5f9a3ecbe5ed12ab4d8e11ba873c2f11161202b945
 * Environment attestation: recorded in the manifest sidecar; it does not affect these bytes.
 * Evidence boundary: Lean certificates prove source-to-model refinement conditionally; engine conformance remains an explicit obligation.
 */
export type TreeMergePolicyData = TreeMergePolicy["kind"];

/**
 * How a merge resolves the tree its two parents share (SPEC §5.2.1): take one side's tree
 * wholesale, or take per path the side that changed it relative to the common ancestor.
 */
export abstract class TreeMergePolicy {
    public static get ours(): TreeMergePolicy {
        return oursTreeMergePolicy;
    }
    public static get theirs(): TreeMergePolicy {
        return theirsTreeMergePolicy;
    }
    public static get perPath(): TreeMergePolicy {
        return perPathTreeMergePolicy;
    }
    public static from(kind: TreeMergePolicy["kind"]): TreeMergePolicy {
        switch (kind) {
            case "ours": return TreeMergePolicy.ours;
            case "theirs": return TreeMergePolicy.theirs;
            case "perPath": return TreeMergePolicy.perPath;
        }
    }
    public static fromData(value: GeneratedData): TreeMergePolicy {
        switch (value) {
            case "ours": return TreeMergePolicy.ours;
            case "theirs": return TreeMergePolicy.theirs;
            case "perPath": return TreeMergePolicy.perPath;
            default: throw new TypeError("TreeMergePolicy data must name a constructor");
        }
    }
    public abstract readonly kind: "ours" | "theirs" | "perPath";
    /**
     * The side a wholesale resolution records, and nothing when resolution is per path. A per-path
     * merge has no single side to record, which is exactly why it is the policy that can surface
     * a conflict.
     */
    public abstract side(): Option<TreeMergeSide>;
    /**
     * Whether a path both sides changed is a conflict the operator resolves explicitly. A
     * wholesale policy has already answered every path by naming a side; only a per-path merge
     * can reach a path whose answer it does not have.
     */
    public abstract surfacesConflicts(): boolean;
    public toData(): TreeMergePolicyData {
        return this.kind;
    }
    public equals(other: TreeMergePolicy): boolean {
        return this === other;
    }
}

class OursTreeMergePolicy extends TreeMergePolicy {
    public readonly kind = "ours" as const;
    public override side(): Option<TreeMergeSide> {
        return { kind: "some", value: "ours" };
    }
    public override surfacesConflicts(): boolean {
        return false;
    }
}

class TheirsTreeMergePolicy extends TreeMergePolicy {
    public readonly kind = "theirs" as const;
    public override side(): Option<TreeMergeSide> {
        return { kind: "some", value: "theirs" };
    }
    public override surfacesConflicts(): boolean {
        return false;
    }
}

class PerPathTreeMergePolicy extends TreeMergePolicy {
    public readonly kind = "perPath" as const;
    public override side(): Option<TreeMergeSide> {
        return { kind: "none" };
    }
    public override surfacesConflicts(): boolean {
        return true;
    }
}

const oursTreeMergePolicy = new OursTreeMergePolicy();

const theirsTreeMergePolicy = new TheirsTreeMergePolicy();

const perPathTreeMergePolicy = new PerPathTreeMergePolicy();

/**
 * The side of a merge a wholesale resolution takes the tree from (SPEC §5.2.1).
 */
export type TreeMergeSide = "ours" | "theirs";

export type GeneratedData = boolean | bigint | number | string | null | undefined | readonly GeneratedData[] | {
    readonly [key: string]: GeneratedData;
};

export type Option<A> = {
    readonly kind: "none";
} | {
    readonly kind: "some";
    readonly value: A;
};
