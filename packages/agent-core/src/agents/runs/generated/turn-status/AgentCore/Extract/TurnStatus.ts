/*
 * Generated by TSLean from kernel-checked Lean declarations.
 * Fragment: tslean-semantic-typed-v6
 * Lean module: AgentCore.Extract.TurnStatus
 * Generated module: AgentCore/Extract/TurnStatus.ts
 * Package entry module: AgentCore.Extract.TurnStatus
 * Semantic identity: sha256:ac3dee936546aa5e12c12aafc257ac4ebc91a8fdc040867fc150a15fe761bd37
 * Input closure: sha256:323392ff2cd7f0a5933fb32915c3b3285b99355438953fa8dafd89670c7fa68a
 * Semantic IR: sha256:5913af21a5a9ce139d32d4044d47464a9f58da440f9108b7785b63922c205fae
 * Generated module body: sha256:bc9bea32910a0cf68e4b444a3d8133436963bee705cc86075a0f8f2332c1f239
 * Generated package body: sha256:f9e202c5633342f4959d86ddec7863b8a88e97a667b0102ea6da4ce9522f309a
 * Lean toolchain: leanprover/lean4:v4.33.1
 * Lean: Lean (version 4.33.1, x86_64-unknown-linux-gnu, commit 819816b2e0a3bf405af45ae5c7af2491d8f5bee6, Release)
 * Lake: Lake version 5.0.0-src+819816b (Lean version 4.33.1)
 * Runtime certificates: sha256:4f53cda18c2baa0c0354bb5f9a3ecbe5ed12ab4d8e11ba873c2f11161202b945
 * Environment attestation: recorded in the manifest sidecar; it does not affect these bytes.
 * Evidence boundary: Lean certificates prove source-to-model refinement conditionally; engine conformance remains an explicit obligation.
 */
/**
 * How a Turn ended (SPEC §5.3). The three outcomes are the whole terminal vocabulary, shared
 * by Turn status, Run settlement, and every record that names an ending.
 */
export type TerminalOutcome = "succeeded" | "failed" | "cancelled";

export type TurnStatusData = TurnStatus["kind"];

/**
 * Where a Turn is in its lifecycle (SPEC §5.3). `queued` and `suspended` are the two statuses
 * a lease may be claimed from; `running` is the only status that may complete; the three
 * terminal statuses admit no further move.
 */
export abstract class TurnStatus {
    public static get queued(): TurnStatus {
        return queuedTurnStatus;
    }
    public static get running(): TurnStatus {
        return runningTurnStatus;
    }
    public static get suspended(): TurnStatus {
        return suspendedTurnStatus;
    }
    public static get succeeded(): TurnStatus {
        return succeededTurnStatus;
    }
    public static get failed(): TurnStatus {
        return failedTurnStatus;
    }
    public static get cancelled(): TurnStatus {
        return cancelledTurnStatus;
    }
    public static from(kind: TurnStatus["kind"]): TurnStatus {
        switch (kind) {
            case "queued": return TurnStatus.queued;
            case "running": return TurnStatus.running;
            case "suspended": return TurnStatus.suspended;
            case "succeeded": return TurnStatus.succeeded;
            case "failed": return TurnStatus.failed;
            case "cancelled": return TurnStatus.cancelled;
        }
    }
    public static fromData(value: GeneratedData): TurnStatus {
        switch (value) {
            case "queued": return TurnStatus.queued;
            case "running": return TurnStatus.running;
            case "suspended": return TurnStatus.suspended;
            case "succeeded": return TurnStatus.succeeded;
            case "failed": return TurnStatus.failed;
            case "cancelled": return TurnStatus.cancelled;
            default: throw new TypeError("TurnStatus data must name a constructor");
        }
    }
    public abstract readonly kind: "queued" | "running" | "suspended" | "succeeded" | "failed" | "cancelled";
    /**
     * The status an unheld cancellation moves this Turn to. Cancelling without a lease token is
     * admitted exactly where no token exists to be presented: a queued or suspended Turn. A
     * running Turn is held, and cancelling it requires that holder's token.
     */
    public abstract cancelUnheld(): Option<TurnStatus>;
    /**
     * The status a claim moves this Turn to, and nothing when a Turn in this status cannot be
     * claimed. A queued Turn starts; a suspended Turn resumes; a running Turn is already held and
     * a terminal Turn is finished, so neither admits a claim.
     */
    public abstract claim(): Option<TurnStatus>;
    /**
     * Whether this Turn may complete with an outcome. Only a running Turn may — a Turn that never
     * started, or already ended, has no attempt to record an outcome for. The status it reaches is
     * `ofTerminalOutcome` of that outcome, which is a fact about the outcome alone and so is
     * decided there; keeping the two apart is also what keeps this dispatch from carrying a
     * parameter half its cases would ignore.
     */
    public abstract completes(): boolean;
    /**
     * The status a suspension moves this Turn to. Only a running Turn suspends: suspending a
     * queued Turn would invent a hold nobody took, and a terminal Turn has nothing to suspend.
     */
    public abstract suspend(): Option<TurnStatus>;
    /**
     * Whether this Turn has ended (SPEC §5.3): terminalization reads exactly this.
     */
    public abstract terminal(): boolean;
    public toData(): TurnStatusData {
        return this.kind;
    }
    public equals(other: TurnStatus): boolean {
        return this === other;
    }
}

export class QueuedTurnStatus extends TurnStatus {
    public readonly kind = "queued" as const;
    public constructor() {
        super();
        Object.freeze(this);
    }
    public override cancelUnheld(): Option<TurnStatus> {
        return { kind: "some", value: TurnStatus.cancelled };
    }
    public override claim(): Option<TurnStatus> {
        return { kind: "some", value: TurnStatus.running };
    }
    public override completes(): boolean {
        return false;
    }
    public override suspend(): Option<TurnStatus> {
        return { kind: "none" };
    }
    public override terminal(): boolean {
        return false;
    }
}

export class RunningTurnStatus extends TurnStatus {
    public readonly kind = "running" as const;
    public constructor() {
        super();
        Object.freeze(this);
    }
    public override cancelUnheld(): Option<TurnStatus> {
        return { kind: "none" };
    }
    public override claim(): Option<TurnStatus> {
        return { kind: "none" };
    }
    public override completes(): boolean {
        return true;
    }
    public override suspend(): Option<TurnStatus> {
        return { kind: "some", value: TurnStatus.suspended };
    }
    public override terminal(): boolean {
        return false;
    }
}

export class SuspendedTurnStatus extends TurnStatus {
    public readonly kind = "suspended" as const;
    public constructor() {
        super();
        Object.freeze(this);
    }
    public override cancelUnheld(): Option<TurnStatus> {
        return { kind: "some", value: TurnStatus.cancelled };
    }
    public override claim(): Option<TurnStatus> {
        return { kind: "some", value: TurnStatus.running };
    }
    public override completes(): boolean {
        return false;
    }
    public override suspend(): Option<TurnStatus> {
        return { kind: "none" };
    }
    public override terminal(): boolean {
        return false;
    }
}

export class SucceededTurnStatus extends TurnStatus {
    public readonly kind = "succeeded" as const;
    public constructor() {
        super();
        Object.freeze(this);
    }
    public override cancelUnheld(): Option<TurnStatus> {
        return { kind: "none" };
    }
    public override claim(): Option<TurnStatus> {
        return { kind: "none" };
    }
    public override completes(): boolean {
        return false;
    }
    public override suspend(): Option<TurnStatus> {
        return { kind: "none" };
    }
    public override terminal(): boolean {
        return true;
    }
}

export class FailedTurnStatus extends TurnStatus {
    public readonly kind = "failed" as const;
    public constructor() {
        super();
        Object.freeze(this);
    }
    public override cancelUnheld(): Option<TurnStatus> {
        return { kind: "none" };
    }
    public override claim(): Option<TurnStatus> {
        return { kind: "none" };
    }
    public override completes(): boolean {
        return false;
    }
    public override suspend(): Option<TurnStatus> {
        return { kind: "none" };
    }
    public override terminal(): boolean {
        return true;
    }
}

export class CancelledTurnStatus extends TurnStatus {
    public readonly kind = "cancelled" as const;
    public constructor() {
        super();
        Object.freeze(this);
    }
    public override cancelUnheld(): Option<TurnStatus> {
        return { kind: "none" };
    }
    public override claim(): Option<TurnStatus> {
        return { kind: "none" };
    }
    public override completes(): boolean {
        return false;
    }
    public override suspend(): Option<TurnStatus> {
        return { kind: "none" };
    }
    public override terminal(): boolean {
        return true;
    }
}

const queuedTurnStatus = new QueuedTurnStatus();

const runningTurnStatus = new RunningTurnStatus();

const suspendedTurnStatus = new SuspendedTurnStatus();

const succeededTurnStatus = new SucceededTurnStatus();

const failedTurnStatus = new FailedTurnStatus();

const cancelledTurnStatus = new CancelledTurnStatus();

/**
 * The status a Turn reaches by ending with this outcome.
 */
export function ofTerminalOutcome(outcome: TerminalOutcome): TurnStatus {
    if (outcome === "succeeded") {
        return TurnStatus.succeeded;
    }
    if (outcome === "failed") {
        return TurnStatus.failed;
    }
    return TurnStatus.cancelled;
}

function requireTerminalOutcome(value: GeneratedData, name: string): TerminalOutcome {
    if (value === "succeeded" || value === "failed" || value === "cancelled") {
        return value;
    }
    throw new TypeError(`${name} must name a TerminalOutcome`);
}

export const TerminalOutcome = Object.freeze({
    fromData(value: GeneratedData): TerminalOutcome {
        return requireTerminalOutcome(value, "TerminalOutcome");
    }
});

export type GeneratedData = boolean | bigint | number | string | null | undefined | readonly GeneratedData[] | {
    readonly [key: string]: GeneratedData;
};

export type Option<A> = {
    readonly kind: "none";
} | {
    readonly kind: "some";
    readonly value: A;
};
